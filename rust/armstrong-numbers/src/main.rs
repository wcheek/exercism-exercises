// use std::str::FromStr;
pub fn main() {
    // let num: u32 = 10;
    // let num_string = String::from_str(&num.to_string()).expect("success");
    // println!("{:?}", num_string);
    // let num_array: Vec<u32> = num_string
    //     .chars()
    //     .map(|s| s.to_digit(10).expect("parse error"))
    //     .map(|s| s.pow(2))
    //     .collect();
    // println!("{:?}", num_array);
    // let armstrong_num = num_array
    //     .iter()
    //     .reduce(|acc, e| &acc + *e.pow(2))
    //     .expect("Error reducing");
    // println!("{}", armstrong_num);
    // num == armstrong_num
}
